<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="replyMapper">

	
	<resultMap id="replyMap" type="Reply">
	 <id  property="replyno"  column="replyno"/>
	<result property="userno" column="userno"/>
	<result property="postno" column="postno"/>
	<result property="textbody" column="textbody"/>
	<result property="regdate" column="regdate"/>
	<result property="state" column="state"/>
	<result property="child_leplyno" column="child_replyno"/>
	</resultMap>
	
	
      <select id="selectAll" resultMap="replyMap">
        SELECT
            *
        FROM
            NUSER
    </select>
     
      <select id="selectLocation"  resultType="Location">
        SELECT
            *
        FROM
            LOCATION
            WHERE
            LOCATIONNO = #{location}
    </select>
     
       <select id="findBy" resultMap="replyMap">
        SELECT
            *
        FROM
            NUSER
        WHERE 
        ${search} = #{keyword}
    </select>
    
     
    
     <insert id="insert" useGeneratedKeys="true"  keyProperty="replyno" parameterType="Map" > 
        INSERT INTO REPLY (userno,postno,postno,textbody,state)
         VALUES (#{userno}, #{postno}, #{postno}, #{textbody},#{state}) 
    </insert>
    
    <update id="updateBy"  > 
         
         UPDATE NUSER
SET 
${colum} = #{value}
WHERE
 userno = #{userno}
    </update>
    
    
    
</mapper>

